# PRERIT THAKUR — Portfolio

Built with Vite + React + Tailwind CSS.

## Develop

Install deps:

npm ci

Run dev server:

npm run dev

Build:

npm run build

## Deploy
This repo contains a GitHub Actions workflow that will deploy the `dist/` build output to GitHub Pages automatically when you push to `main`.
